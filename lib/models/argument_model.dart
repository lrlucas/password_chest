import 'package:app_password_chest/enum/page_mode.dart';
import 'package:app_password_chest/models/pass_model.dart';

class ArgumentModel {
  PageMode? pageMode;
  PassModel? passModel;
  ArgumentModel({this.pageMode, this.passModel});
}
